{
  "name": "tokenised-lse",
  "version": "1.0.0",
  "private": true,
  "description": "Tokenised LSE exchange prototype smart contracts and tooling.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "dev:chain": "sh -c 'npx hardhat node > logs/hardhat.log 2>&1 & sleep 1 && npx hardhat run scripts/deploy-ttoken.js --network localhost && npx hardhat run scripts/deploy-listings.js --network localhost && npx hardhat run scripts/deploy-orderbook.js --network localhost && npx hardhat run scripts/deploy-dividends.js --network localhost && npx hardhat run scripts/deploy-leveraged.js --network localhost'"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.20.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
